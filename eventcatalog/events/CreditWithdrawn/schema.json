{
    "$id": "https://cosmonic.com/concordance/mutualcreditaccount/CreditWithdrawn.json",
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "CreditWithdrawn",
    "type": "object",
    "properties": {
      "accountNumber": {
        "type": "string",
        "description": "The account number"
      },
      "amount": {
        "type": "integer",
        "description": "The amount withdrawn"
      },
      "note": {
        "type": "string",
        "description": "An optional note associated with the withdrawal"
      },
      "entityId": {
        "type": "string",
        "description": "The ID of the entity that performed the withdrawal"
      }    
    },
    "required": ["accountNumber", "amount", "entityId"]
  }
  